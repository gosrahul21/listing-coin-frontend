export enum UserRoleType {
    ADMIN = 'admin',
    TRANSPORTER = 'transporter',
    CERTIFIER = 'certifier',
    REGULATOR = 'regulator',
    PRODUCER = 'producer',
    DISTRIBUTOR = 'distributor',
    CONSUMER = 'consumer'
}
